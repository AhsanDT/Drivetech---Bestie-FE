import {StyleSheet} from 'react-native';
import {colors, WP, family, sie} from '../../../shared/exporter';

const styles = StyleSheet.create({
  mainContainer: {
    flex: 1,
    backgroundColor: colors.white,
  },

});

export {styles};
